describe('Step Logo', () => {
  it('Step은 정상적으로 렌더링 된다.', () => {
    // const { getByTestId } = render(<Step current={0} total={10} />)
    // expect(getByTitle('symbol logo')).toBeInTheDocument()
  })
})
